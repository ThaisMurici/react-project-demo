import React from "react";

import { HeaderContainer, HeaderTitle } from "./styles";

function MainHeader() {
  return (
    <HeaderContainer>
      <HeaderTitle>App Demo: React + Redux + Saga</HeaderTitle>
    </HeaderContainer>
  );
}

export default MainHeader;
